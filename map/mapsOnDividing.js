// Given an array of numbers, create a new array where each number is divided by 5


const numbers = [50, 100, 150, 200, 250, 300, 350, 400, 450, 500];  //array has been define

const newNumbers=numbers.map(numbs=>numbs/5);                           //map function has been used

// const newNumbers=numbers.map(numbs=>{ return numbs/5});

console.log(newNumbers);                           //console has been print